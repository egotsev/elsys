#include <iostream>

class Board {
    int cells_[8];
    
    void putQueen(int col, int row) {
        if (!is_beaten(col, row)) {
        
        }
    }
    
    bool is_beaten(int col, int row) {
        for(int i = 0; i<8; i++) {
            
        }
    }
};

int main() {
    return 0;
}
